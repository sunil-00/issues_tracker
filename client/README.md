### Frontend Setup (React)

1. **Install dependencies:**

    ```bash
    npm install
    ```

2. **Run the React app:**

    ```bash
    npm run dev
    ```

The server should now be running at `http://localhost:5173.`